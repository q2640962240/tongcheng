# 白夜 — 领域模型

> 纯概念定义，不含实现细节。代码可以重构，术语不变。
> 当术语与本文冲突时，以本文为准，除非本文被显式更新。

---

## 术语表

### 用户角色

**用户 (User)**
使用白夜 App 的人。拥有钻石余额、魅力值、礼物收入等属性。

**管理员 (Admin)**
通过管理后台操作系统的人。可审核提现、管理礼物库、调整用户余额、查看聊天/送礼记录。

**AI 用户 (AI User)**
系统预置的虚拟用户，用于在真实用户不在线时自动回复消息，维持社交氛围。以特定用户身份发送消息。

---

### 经济体系

**钻石 (Diamond)**
用户通过充值获得的虚拟货币。送礼时消耗。不可逆向兑换为现金。

**礼物收入 (Gift Income)**
用户收礼后获得的收入，单位为**分**（1 元 = 100 分）。可通过提现转为现金。

**魅力值 (Charm Value)**
用户累计收到的钻石总数，只增不减。用于排行榜展示，衡量用户的社交吸引力。

**充值 (Recharge)**
用户用真实货币购买钻石的行为。当前需管理员手动调整余额，待接入支付。

**提现 (Withdraw)**
用户将礼物收入转为现金的行为。前提：已绑定收款账号。需管理员审核。

**收款绑定 (Payment Binding)**
用户绑定的支付宝/微信收款账号及二维码。提现的前置条件。换绑需短信验证。

**交易记录 (Transaction)**
钻石或礼物收入变动的审计日志。类型包括：gift_send、gift_receive、withdraw、admin_adjust、recharge 等。

---

### 社交体系

**会话 (Conversation)**
两个用户之间的聊天通道。对应腾讯 IM 的一个 C2C 会话。

**消息 (Message)**
会话中的一条记录。类型包括：text（文本）、image（图片）、gift（礼物）、system（系统通知）。

**礼物消息 (Gift Message)**
一种特殊消息，携带礼物信息。在聊天中触发双方可见的动画效果。

---

### 礼物系统

**礼物 (Gift)**
可赠送的虚拟物品，有名称、价格（钻石）、动画等级。管理员可增删改。

**动画等级 (Animation Level)**
礼物的视觉特效级别：
- L0: 无动画
- L1: 小飘动 + 左侧信息条
- L2: 光柱 + 弹性礼物 + 旋转光圈
- L3: 全屏粒子 + 烟花 + 底部横幅

**送礼记录 (Gift Record)**
一次送礼行为的持久化记录：发送方、接收方、礼物、数量、关联消息。

**礼物排行榜 (Gift Rank)**
两种排名：魅力榜（收钻石最多）和豪礼榜（送钻石最多）。支持日/周/总三种时间维度。

---

### 聊天架构

**主通道 (Primary Channel)**
腾讯官方 TUIKit SDK 提供的聊天能力。IM 登录成功后自动进入。

**兜底通道 (Fallback Channel)**
自建 Socket.IO 聊天通道。当 TUIKit IM 登录失败时降级使用。

**IM 桥接 (IM Bridge)**
将自建通道或 AI 发出的消息，通过腾讯 IM v4 REST API 转发到对方，确保消息可达。

**IM 同步 (IM Sync)**
TUIKit 发出的消息，通过腾讯 IM 回调 → 服务端 API 写入数据库，保持 DB 与 IM 云一致。

---

### 组局系统

**组局 (Group)**
用户发起的线下/线上活动。有标题、描述、时间、地点、人数限制。其他用户可申请加入，发起者审批。

---

### 运营体系

**Banner**
首页轮播广告位。管理员可配置图片、链接、排序。

**分类 (Category)**
服务/约玩的分类标签。

**约玩 (Play/Service)**
用户发布的陪玩服务信息，有标题、价格、描述、分类。

**每日任务 (Daily Task)**
用户每天可完成的任务（签到、聊天、送礼等），完成后获得奖励。

---

## 关系

- 一个 **用户** 拥有多个 **会话**
- 一个 **会话** 包含多条 **消息**
- 一条 **礼物消息** 对应一条 **送礼记录**
- 一次 **送礼** 产生一条 **交易记录**（发送方扣钻石）和一条 **交易记录**（接收方加收入）
- **魅力值** = 累计收到的钻石总和
- **提现** 前置条件：已 **收款绑定**
- **组局** 有一个发起者和多个申请者

---

## 歧义澄清

| 容易混淆 | 正确理解 |
|---|---|
| "钻石" vs "礼物收入" | 钻石是支出货币（充值获得），礼物收入是收入货币（收礼获得），两者不可互转 |
| "魅力值" vs "礼物排行榜" | 魅力值是个人累计属性（只增不减），排行榜是时段性排名 |
| "消息" vs "礼物消息" | 礼物消息是消息的子类型，携带额外礼物数据 |
| "约玩" vs "组局" | 约玩是 1v1 服务，组局是 1vN 活动 |
| "主通道" vs "兜底通道" | 主通道用腾讯 IM SDK，兜底用自建 Socket.IO，正常情况走主通道 |
